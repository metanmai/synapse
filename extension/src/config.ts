// Prod endpoints — mirror mcp/src/cli/config.ts (the canonical CLI values).
//   API_URL — backend Worker: /auth/cli-exchange + /api/capture/browser
//   APP_URL — frontend: the /cli-auth sign-in page the extension opens
export const API_URL = "https://api.synapsesync.app";
export const APP_URL = "https://synapsesync.app";
