---
quick_id: 260621-h13
slug: billing-subscription-tests
status: complete
date: 2026-06-21
---

# Quick Task 260621-h13: Billing/subscription test coverage

## Problem

The billing/subscription happy path is untested. Existing coverage:
- `creem.test.ts` — HMAC + creemRequest ✅
- `tier-enforce.test.ts` — Free/Plus quota enforcement ✅
- `billing-webhook-dispatch.test.ts` — ONLY the `default:` branch + renewal-gap breadcrumb (its own comment claims the fake `db` is a no-op — **stale**: the dispatcher passes `db` through, so transitions ARE observable)
- `billing.test.ts` — ONLY negative paths (400 bad-sig, 401 no-auth)

**Gap:** no test asserts (1) webhook event → correct subscription state transition, (2) subscription→tier resolution, (3) the `getActiveSubscription` status filter / `upsertSubscription` conflict target, (4) a valid-HMAC webhook reaching dispatch through the real worker.

Guard the **bug class** (the lifecycle transition matrix + the gating filter), not specific instances.

## Out of scope

Live-Creem checkout e2e (real checkout → real webhook delivery) — needs Creem test keys + a public webhook URL. That's an owner-side rehearsal, tracked separately.

## Tasks

1. **`backend/test/api/billing-webhook-transitions.test.ts`** — `dispatchCreemWebhookEvent` state transitions via `makeMockSupabase` `calls` recorder. Table-driven over the lifecycle: `checkout.completed`→active, `subscription.active|paid`→active, `canceled|expired`→inactive, `past_due`→past_due, `scheduled_cancel`→cancel_at_period_end+status-unchanged. Plus guards: missing `synapse_user_id`→no write; lifecycle event for unknown sub→no write (don't fabricate a row); unknown event (`invoice.paid`)→`handled:false` + no write.

2. **`backend/test/db/subscriptions-queries.test.ts`** — recording mock that captures filter args. `getActiveSubscription` MUST apply `.eq("user_id", …)` + `.in("status", ["active","past_due"])` (the gating filter — guards drift to include `inactive` or drop `past_due`). `upsertSubscription` MUST use `{ onConflict: "provider_subscription_id" }` + correct defaults. `getTierForUser` maps row→`plus`, null→`free`.

3. **`backend/test/api/billing-webhook-integration.test.ts`** — vi.mock Supabase, sign a body with HMAC-SHA256 (same algo as `verifyCreemWebhook`), POST `/api/billing/webhook`. Valid sig + `checkout.completed` → 200 + upsert recorded. Valid sig + unknown event → 200 (Creem must get 200 to stop retrying) + NO write (pins the silent-renewal behavior at the HTTP layer).

## Verify

`cd backend && npx vitest run` green; full `npm run test` green before commit. CI must stay green.
